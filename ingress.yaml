apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: notre-application-ingress
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  ingressClassName: traefik
  rules:
  - host: notreapp.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: notre-application-service
            port:
              number: 80
